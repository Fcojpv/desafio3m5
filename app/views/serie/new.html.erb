<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="bg-dark p-3 rounded mb-4">
      <h1 class="text-white mb-0">Nueva Serie</h1>
    </div>

    <%= form_with(model: @serie, url: series_create_path, local: true) do |form| %>
      <% if @serie.errors.any? %>
        <div class="alert alert-danger">
          <h2><%= pluralize(@serie.errors.count, "error") %> impidieron guardar esta serie:</h2>
          <ul>
            <% @serie.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="mb-3">
        <%= form.label :name, "Nombre", class: "form-label text-white" %>
        <%= form.text_field :name, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :synopsis, "Sinopsis", class: "form-label text-white" %>
        <%= form.text_area :synopsis, class: "form-control", rows: 4 %>
      </div>

      <div class="mb-3">
        <%= form.label :director, "Director", class: "form-label text-white" %>
        <%= form.text_field :director, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.submit "Crear Serie", class: "btn btn-primary" %>
        <%= link_to "Cancelar", series_path, class: "btn btn-secondary" %>
      </div>
    <% end %>
  </div>
</div>
